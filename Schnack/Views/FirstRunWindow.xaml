<Window x:Class="Schnack.Views.FirstRunWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="360" Width="460"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True">
    <StackPanel Margin="24">
        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="LanguageLabel" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="LanguageBox" Grid.Column="1"
                      DisplayMemberPath="Display"
                      SelectionChanged="OnLanguageChanged"/>
        </Grid>

        <TextBlock x:Name="IntroText" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,12"/>
        <TextBlock x:Name="Step1Text" TextWrapping="Wrap" Margin="0,0,0,8"/>
        <TextBlock x:Name="Step2Text" TextWrapping="Wrap" Margin="0,0,0,16"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OpenSettingsButton" Width="180" Margin="0,0,8,0" Click="OnOpenSettingsClick"/>
            <Button x:Name="CloseButton" Width="90" IsDefault="True" Click="OnCloseClick"/>
        </StackPanel>
    </StackPanel>
</Window>
