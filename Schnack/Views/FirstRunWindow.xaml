<Window x:Class="Schnack.Views.FirstRunWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Willkommen bei Schnack"
        Height="320" Width="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True">
    <StackPanel Margin="24">
        <TextBlock TextWrapping="Wrap" FontSize="14" Margin="0,0,0,12"
                   Text="Schnack wurde eingerichtet. Bevor du diktierst:"/>
        <TextBlock TextWrapping="Wrap" Margin="0,0,0,8"
                   Text="1. In den Einstellungen ein Backend wählen: OpenAI (Standard, Cloud) oder Claude (lokales Whisper + Anthropic)."/>
        <TextBlock TextWrapping="Wrap" Margin="0,0,0,16"
                   Text="2. Den passenden API-Key hinterlegen: OPENAI_API_KEY bzw. ANTHROPIC_API_KEY als Umgebungsvariable oder in den Einstellungen speichern."/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Einstellungen öffnen…" Width="160" Margin="0,0,8,0" Click="OnOpenSettingsClick"/>
            <Button Content="Schließen" Width="90" IsDefault="True" Click="OnCloseClick"/>
        </StackPanel>
    </StackPanel>
</Window>
