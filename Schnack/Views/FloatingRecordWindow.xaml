<Window x:Class="Schnack.Views.FloatingRecordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Schnack"
        Width="52" Height="52"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SnapsToDevicePixels="True">

    <Border x:Name="RootBorder"
            Width="48" Height="48"
            CornerRadius="24"
            Background="#F5FFFFFF"
            BorderBrush="#CC333333"
            BorderThickness="1"
            RenderTransformOrigin="0.5,0.5"
            MouseLeftButtonDown="OnBorderMouseLeftButtonDown"
            MouseMove="OnBorderMouseMove"
            MouseLeftButtonUp="OnBorderMouseLeftButtonUp">
        <!-- Rechtsklick öffnet ein Bestätigungs-Menü statt sofort auszublenden;
             Klick außerhalb schließt das Menü ohne Aktion. -->
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem x:Name="CloseMenuItem" Click="OnCloseMenuItemClick"/>
            </ContextMenu>
        </Border.ContextMenu>
        <Border.RenderTransform>
            <ScaleTransform x:Name="RootScale" />
        </Border.RenderTransform>
        <Image x:Name="LogoImage" Source="/Resources/Schnack_Logo.png"
               Width="32" Height="32"
               Stretch="Uniform"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               IsHitTestVisible="False"/>
    </Border>
</Window>
