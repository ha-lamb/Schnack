---
typ: wissen
projekt: {PROJEKT}
aliases: [{SCHÖNER_TITEL}]
quelle: "{QUELLE_PFAD}"
quelle_hash: {HASH}
export: {DATUM}
generiert: true
tags: [projekt/{projekt-slug}, typ/wissen, memory/{memory-typ}]
---

> [!warning] Generiert vom Skill `obsidian-export`. Änderungen hier werden beim nächsten Export überschrieben. Führend ist `{QUELLE_PFAD}`.

# {SCHÖNER_TITEL}

{Der Body der Memory-Datei, inhaltlich UNVERÄNDERT übernommen — nicht umformulieren,
nicht kürzen, nicht ergänzen. Die [[wikilinks]] darin bleiben wörtlich stehen; sie
treffen die Wissensnotizen, weil deren Dateinamen den Memory-Slugs entsprechen.}
